# Private medical & personal data — never commit
Clinical Records/
Physique Progress/
Trackers & Briefs/*.xlsx

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Node (only if a build step is ever added)
node_modules/

# Food seed importer — DB snapshots (may contain personal log data)
data/food-seed/backups/
